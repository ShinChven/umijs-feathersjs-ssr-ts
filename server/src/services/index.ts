import { Application } from '../declarations';
// Don't remove this comment. It's needed to format import lines nicely.

// noinspection JSUnusedLocalSymbols @ts-ignore
export default function (app: Application): void {
}
